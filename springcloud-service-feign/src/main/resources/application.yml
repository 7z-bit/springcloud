server:
  port: 8081

spring:
  application:
    name: springcloud-service-feign

eureka:
  client:
    service-url:
      defaultZone: http://eureka.com:8761/eureka/

feign:
  hystrix:
    enabled: true